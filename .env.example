# Vercel Basic Auth variables.
# Set these in Vercel Project Settings -> Environment Variables.
BASIC_AUTH_USER=your-user-name
BASIC_AUTH_PASSWORD=replace-with-a-strong-password

# GitHub Pages workflow sets this automatically.
# Do not set DEPLOY_TARGET on Vercel, otherwise Middleware will be disabled.
# DEPLOY_TARGET=github-pages
